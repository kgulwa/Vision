<%= turbo_stream.replace "comment_#{@comment.id}" do %>
  <%= render "pins/comment", comment: @comment %>
<% end %>

<turbo-stream action="redirect" url="<%= pin_path(@pin, anchor: "comments") %>"></turbo-stream>
