<%= turbo_stream.replace "pin_comments_#{@pin.id}" do %>
  <%= render partial: "pins/comments", locals: { pin: @pin } %>
<% end %>

<%= turbo_stream.replace "comment_count_#{@pin.id}" do %>
  <%= @pin.comments.where(parent_id: nil).count %>
<% end %>
